name: copypastefrontend
services:
  website:
    image: node
    working_dir: /app
    restart: always
    ports:
      - "${WEBSITE_PORT}:3000"
    environment:
      - REACT_APP_SERVER_URL=${REACT_APP_SERVER_URL}
    volumes:
      - ./app:/app
    entrypoint: ["sh", "-c", "chmod +x ./entrypoint.sh && ./entrypoint.sh"]
